.m-items
  .m-items__container
    .m-items__container__new
      新着アイテム
      .m-items__container__new__more
        = link_to "#" do
          もっと見る
          %i.fa.fa-chevron-right
    .m-items__container__products.fadein
      - @items.each do |item|
        = link_to item_path(item.id) do
          .m-items__container__products__border
            .m-items__container__products__product
              - if item.buyer.present?
                .m-sold-icon
                  %span SOLD
              .m-items__container__products__product__image
                = image_tag item.images.first.image.url, width: "188px", height: "188px"
                %span.price= "¥#{item.price.to_s(:delimited)}"
              .m-items__container__products__product__caption
                .m-items__container__products__product__caption__text
                  = item.name
